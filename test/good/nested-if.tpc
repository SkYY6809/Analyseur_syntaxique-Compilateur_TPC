int sign(int n) {
    if (n > 0)
        return 1;
    else
        if (n < 0)
            return -1;
        else
            return 0;
}
int main() {
    putint(sign(42));
    putint(sign(-7));
    putint(sign(0));
    return 0;
}
